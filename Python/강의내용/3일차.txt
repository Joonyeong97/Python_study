Data -> Machine Learning
머신러닝은 
입력(매개변수) = 변수 ,배열(대용량처리) ,List, dict(검색용이)
처리 = 연산자(산술,관계) *빼기 , 내장산술함수
sum,pow,sin,cos,tan 등
제어문 (판단문, 반복문)
사용자 정의함수 
매개변수의 종류
required = 순서가 있고 반드시 지정해줘야함
default_add(a=10,b=20)
key_순서를 바꿀 구 있음 add(b=20,a=10)
변동매개변수 *list (별표하나는) , **dict (별표 두개는)

overloading = 함수이름은 같은데 함수의 매개변수의 데이터 타입이 다르거나
매개변수가 다르면 다른 함수로 인식한다.

overriding = 상속후 재정의
같은 이름을 가지고있는 부모를 만들어서 자식에게 상속하고
기능은 같음 하지만 자식에게는 고유한 능력을 갖게하는것

구조적 프로그래밍
순서적 프로그래밍
객체지향 프로그래밍(data + 함수) 멤버변수, 멤버함수, method

일반화 프로그래밍(대표타입지정),template

함수화 프로그래밍 ( for + 함수 )

함수는 1급함수 ( 함수가 변수에 대입이 되기도 하고 매개변수에 데이터로 넘겨줄수도 있고
리턴값으로 쓸수도있음.)
오버로딩시 함수이름이 중복되면 안됨.

데이터 분석은
분류 예측 추천