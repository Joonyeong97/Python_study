김종호
강사님 메일
iadslba@naver.com
https://blog.naver.com/iadslba


list : 매개변수가 길고짧든 데이터 보관

python : 범용언어, 데이터 타입을 지정 X, 전부가 포인터형태라서.
데이터 타입은 주소가 저장되기때문에 반드시 초기화를 해줘야함.

data type
list [  ]  = 메모리 효율적 , 삽입삭제 용이 / 속도가느림
diot {  }
set,  { }
tuple = (  )   수정불가 , 매개변수, 함수  return

numpy = vector(거리크기값) , matrix / ndarray + 선형 개수
모든문제를 선형으로 품 이유는 일반화가 쉽기때문에.
선형회귀 논리는 일반화를 위해서
과적합은 값이 변화면 많이변하기때문에..ㅎ

program (함수베이스)

프로그래밍은 함수가 함수를 호출하는 방식
stack  = First in last Out
만들었다가 줄었다가 반복하면서

heap = memory 할당
실행되면 stack에 저장되고있는 지정된 주소로 이동

function 함수이름( 매개변수 ) , {
return;
}

함수가 하는일
매개변수, 기억변수의 값을가지고 일을함
함수가 하는이릉 저장하면 실제 값은 heap 에저장
거기에 대한 주소를 스택에저장 그 후에  변수에 저장이됨.

mongodb
bigdata ( 분산 처리 ) 

하둡
한 컴퓨터에서 처리된 값을
여러컴퓨터에서 바로 연동해서 처리.


python 툴팁
아나콘다
이클립스
pycharm
비주얼스튜디오









