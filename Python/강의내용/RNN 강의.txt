기본적으로
NN 과 RNN의 차이점
입력층 -> 은닉층 ->출력층 -> 손실함수
는 같음


신경망을 그대로 사용하는건 같음
일반 신경망에서는  relu를 통과후에 바로 출력층으로 가는반면

RNN은
은닉층에서 순환구조되어 tanh으로 다시 값이 전달됩니다.
동시에 출력층에도 값이 전달됩니다.

내부적으로 순환 (recurrent) 되는 구조를 이용하여
순서(sequence)가 있는 데이터를 처리하는 데 강점을 가진 신경망
순서가 있는 데이터??

순서가 있는 데이터
문장이나 음성 같은 연속적인 데이터를 말하는데, 이런데이터는
문장에서 놓여진 위치(순서)에 따라 의미가 달라지는 것을 알수 있음

즉, 현재 데이터 의미를 알기 위해서는 이전에 놓여있는 과거 데이터도
알고있어야함

그래서 RNN은 이러한 과거의 데이터를 알기 위해서 
1. 은닉층내에 순환(Recurrent) 구조를 이용하여 과거의 데이터를
기억해두고 있다가
**2. 새롭게 입력으로 주어지는 데이터와 은닉층에서 기억하고 있는
과거 데이터를 연결 시켜서 그 의미를 알아내는 기능을 가지고 있음.

I work at google

i 가 입력되면 그걸 기억하고있다가
work 하고 연결해서 출력을 진행 이때 소프트맥스를 이용하여
확률로 나타내줌
대명사,동사,전치사,명사 등으로 확률적으로 표현

at이 들어오면 I 에 영향을 받은 work 와 연결하여 판단

google 이 들어오면 I + work 에 영향을 받은 at과 연결되어 판단후
출력함.

시간 개념을 포함한 RNN 구조

순환 구조를 은닉층에서 기억하는 과거의 데이터와 일정시간이
지난후에 입력되는 데이터에 연결시켜 주는 구조로 바꿔서
생각해 볼수있음.

즉, 문장이나 음성 같은 순서가 있는 데이터라는 것은, 시간의 경과에 따라서
데이터가 순차적으로 들어온다는 것과 같은 의미라는 것을 알수 있음.


바이어스는 각층에서 오직 1개의 값으로 정의될 수 있으나,
가중치는 각 층(layer)으로 입력되는 데이터의 개수만큼 정의되는것을
알수있음

바이어스값은 회귀값 이후에 더해짐
ex) 입력층 -> A1 입력 -> 가중치W1과의 행렬곱(linear regression) Z2 ->Relu(A2)  = Z2+B1(바이어스값) 
->출력층으로 가면서 A2·W2 행렬곱으로 다시 linear후 Z3값으로 -> softmax(Z3+B2 ) =A3( 예측값 )
A1·W1 =Z2