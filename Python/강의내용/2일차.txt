입력(매개변수) -> 처리(Block) -> 출력(return)
산술연산자 + - * / 
hab = hab + a
a+=1
관계연산자 true, false
논리연산자 and, or , not

조건문, 반복문, 규칙
구조적으로 함수베이스로 처리하게끔 되어있음
함수는 호출해준곳으로 돌아가는것을 리턴한다고함.

배열은 연속형 데이터
list는 한메모리 첫과 끝에 주소가적혀있어서 삭제가되면 다른것과 연결이됨.

CRUD
Create
Read
Update
delete


insert ,append

dict의 데이터구조는 키이:데이터 로
검색이 용이함 키이만있으면 찾을수있어서
관계형 데이터 베이스 -> NOSQL 

객체 = (변수,함수) 
overloading = 함수이름은 같은데 함수의 매개변수의 데이터 타입이 다르거나
매개변수가 다르면 다른 함수로 인식한다.

overriding = 상속을 허용
같은 이름을 가지고있는 부모를 만들어서 자식에게 상속하고
기능은 같음 하지만 자식에게는 고유한 능력을 갖게하는것

빅데이터를 사용할땐
numpy(nd array)(matrix,vector) -> pandas(Data.frame)
statsmodel = 상관분석,회귀식
사이파이

표준편차, 표준오차

